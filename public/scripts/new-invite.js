const express = require('express');
const router  = express.Router();
const database = require('../db/database');


$(document).ready(() => {



});



router.get("/", (req, res) => {
  console.log(req);
});
